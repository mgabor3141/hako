# Baseline scores

Reference Terminal-Bench results for hako's shipped pi config. Update this when a
config change moves the numbers; cite it in the PR. Raw transcripts stay local
(`results/` is gitignored) — only the summary + pins live here.

| Date | TB dataset | pi version | hako ref | Model | Variant | Resolved | Attempts |
|------|-----------|-----------|----------|-------|---------|----------|----------|
| _TBD_ | terminal-bench@2.0 | _pin_ | _commit_ | _model_ | baseline | _ / 89_ | _n_ |
| _TBD_ | terminal-bench@2.0 | _pin_ | _commit_ | _model_ | goal | _ / 89_ | _n_ |

> No reference run recorded yet. Run `eval/run.sh` (see `eval/README.md`) and
> fill the table once the harness is validated end-to-end.
